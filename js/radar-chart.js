/**
 * @fileoverview Radar Chart — DEPRECATED
 * This module is no longer used. Kept for backwards compatibility.
 * @module RadarChart
 * @memberof DRC
 * @deprecated Risk Profile feature removed
 */
'use strict';

DRC.RadarChart = (() => {
    const init = () => {};
    const render = () => {};
    const AXES = [];
    return { init, render, AXES };
})();
